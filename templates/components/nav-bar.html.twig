<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<div class="container-fluid">
		<a class="navbar-brand" href="/">Navbar</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				<li class="nav-item">
					<a class="nav-link active" aria-current="page" href="/">Home</a>
				</li>
				<li class="nav-item">
					<a class="nav-link active" aria-current="page" href="{{path("offers.list")}}">Offres</a>
				</li>

				{% if not is_granted("IS_AUTHENTICATED_FULLY") %}
					<li class="nav-item">
						<a class="nav-link active" aria-current="page" href="{{path("security.register")}}">Register</a>
					</li>
					<li class="nav-item">
						<a class="nav-link active" aria-current="page" href="{{path("security.login")}}">Login</a>
					</li>
				{% endif %}
				{% if is_granted("IS_AUTHENTICATED_FULLY") %}
					{% if is_granted("ROLE_ADMIN") %}
						<li class="nav-item">
							<a class="nav-link active" aria-current="page" href="{{path("admin")}}">Admin</a>
						</li>
						<li class="nav-item">
							<a class="nav-link active" aria-current="page" href="{{path("admin.contracts.create")}}">create contract</a>
						</li>
						<li class="nav-item">
							<a class="nav-link active" aria-current="page" href="{{path("admin.contractTypes.create")}}">create contractType</a>
						</li>
						<li class="nav-item">
							<a class="nav-link active" aria-current="page" href="{{path("admin.offers.create")}}">create offer</a>
						</li>
					{% endif %}
					<li class="nav-item">
						<a class="nav-link active" aria-current="page" href="{{path("security.logout")}}">logout</a>
					</li>
				{% endif %}

			</ul>
			<form class="d-flex">
				<input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
				<button class="btn btn-outline-success" type="submit">Search</button>
			</form>
		</div>
	</div>
</nav>
